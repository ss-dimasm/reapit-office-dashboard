query GetOffices {
  OfficeData: GetOffices(pageSize: 100, sortBy: "modified") {
    pageSize
    pageNumber
    totalCount
    _embedded {
      id
      name
      manager
      email
      workPhone
      created
    }
  }
}
