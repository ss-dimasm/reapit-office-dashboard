query NegotiatorScreen($OfficeId: [String!]) {
  GetNegotiators(officeId: $OfficeId) {
    totalCount
    _embedded {
      id
      name
      jobTitle
      workPhone
      email
      created
    }
  }
}
